#include <stdio.h>

/**
 * main - start a program that prints all cases
 *
 * Return: always 0 (correct)
 *
 */
int main(void)
{
	char r;
	char s;

	for (r == 'a'; r <= 'z'; r++)
	{
		putchar (r);
	}
	for (s == 'A'; s <= 'Z'; s++)
	{
		putchar (s);
	}
	putchar ('\n');
	return (0);
}
