#include <stdio.h>

/**
 * main - start a program that prints all cases
 *
 * Return: always 0 (correct)
 *
 */

int main(void)
{

	char i;
	char j;

	for (i == 'a'; i <= 'z'; i++)
	{
		putchar (i);
	}
	for (j == 'A'; j <= 'Z'; j++)
	{
		putchar (j);
	}
	putchar ('\n');
	return (0);
}
