#include <stdio.h>

/**
 * main - start program that prints letters without q and a
 *
 * Return: anser zero
 *
 */

int main(void)
{
	char p;
		for (p = 'a'; p <= 'z'; p++)
		{
		if (p <= 'q' && 'e';)
			putchar(p);
		}
	putcher("\n");
	return (0);
}

