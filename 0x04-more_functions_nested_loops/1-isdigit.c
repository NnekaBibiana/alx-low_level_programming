#include "main.h"
/**
 * _isdigit - Entry point
 * @c: parameter to be used
 * Return: Always 0
 */
int _isdigit(int c)
{
	c = 48;

	if (c <= 57)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
