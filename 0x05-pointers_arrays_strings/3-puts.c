#include "main.h"

/**
 * _puts - prints a string
 * @str: prints
 * Return: length
 */

void _puts(char *str)
{
	int = 0;

	while (str[i] != '\0')
	{
		_putchar(str[i]);
		i++;
	}

	_putchar('\n');
}

