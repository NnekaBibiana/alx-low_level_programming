#include<stdio.h>
/**
 * main- serve as the entry point for the program
 *
 * Return: by default zero if no errors otherwise return non-zero value
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
